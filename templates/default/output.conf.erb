[tcpout]
defaultGroup = splunkssl

[tcpout:splunkssl]
server = <%= @splunk_servers.join(', ')%>
sslCertPath = $SPLUNK_HOME/etc/auth/server.pem
sslPassword = password
sslRootCAPath = $SPLUNK_HOME/etc/auth/cacert.pem
sslVerifyServerCert = false
